"use client";
import { saveCompanySettings } from "@/actions/imobiliaria";
import { useActionState } from "react";
const labels: Record<string, string> = { razao_social:"Razão social", nome_fantasia:"Nome fantasia", cnpj:"CNPJ", logradouro:"Logradouro", numero:"Número", complemento:"Complemento", bairro:"Bairro", cidade:"Cidade", uf:"UF", cep:"CEP", creci:"CRECI", signatory_name:"Nome de quem assina", signatory_title:"Cargo de quem assina", document_label:"Identificação do documento" };
export function SettingsForm({ initial }: { initial: Record<string, any> | null }) { const [state, action, pending] = useActionState((_: unknown, formData: FormData) => saveCompanySettings(formData), null); const fields = Object.keys(labels); return <form action={action} className="card stack"><div><h2 style={{ margin:0 }}>Dados da imobiliária</h2><p className="muted">Esses dados aparecem em todos os recibos gerados.</p></div>{state?.error && <div className="notice error">{state.error}</div>}{state?.success && <div className="notice success">Configuração salva.</div>}<div className="form-grid">{fields.map((field) => <label className="field" key={field}>{labels[field]}<input name={field} defaultValue={initial?.[field] || (field === "document_label" ? "DIMOB" : field === "signatory_title" ? "Sócia – Administrativa" : "")}/></label>)}</div><div><button className="button" disabled={pending}>{pending ? "Salvando..." : "Salvar configuração"}</button></div></form>; }
